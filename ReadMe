This folder consists of two python files:
stereo_.py and utils.py

stereo_ is the main file with all the necessary functions in utils.


The necessary information about each stereo pair is present in the Stereo_ as global variables. 
To run the file:
Unzip the folder. Run the stereo_.py file and observe the results.
To perform the same operations on different stereo pair, pls add the image pair in data folder->nameOfimage -> Insert left and right image
In the stereo_.py file, add the K matrix and baselinevalues and give it corresponding flag number to work with.
